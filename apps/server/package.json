{
  "name": "@agentgrid/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "eslint src",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentgrid/shared": "workspace:*",
    "@fastify/websocket": "^11.0.2",
    "fastify": "^5.2.1",
    "node-pty": "^1.0.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/ws": "^8.18.0",
    "tsx": "^4.19.3",
    "vitest": "^3.0.8"
  }
}
